
import { productionSite } from 'portfolio/helpers/production-site';
import { module, test } from 'qunit';

module('Unit | Helper | production site');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productionSite([42]);
  assert.ok(result);
});

